# Copy to .env.local and fill in. Never commit the real .env.local file.
# Public (NEXT_PUBLIC_*) values only — Hunar and NVIDIA keys are server-side-only
# and must never be defined here. See ../CLAUDE.md.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
